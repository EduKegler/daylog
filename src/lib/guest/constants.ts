export const GUEST_SESSION_COOKIE = "guest_session_id";
export const GUEST_TIMEZONE_COOKIE = "guest_timezone";

export const GUEST_SESSION_TTL_DAYS = 30;

export const DEFAULT_TIMEZONE = "America/Sao_Paulo";

export const GUEST_QUOTAS = {
  dailyTasksPerDay: 20,
  recurringTasksTotal: 5,
} as const;
